// Display the argument in a message box in the browser 
var alertMessage = function(message){
	alert(message);
};

// Export the functions to use them as a module
exports.alertMessage = alertMessage;